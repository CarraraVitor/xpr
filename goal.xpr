// XPR: A STUDY IN eXPRessions
// Lexer       (Raw Text  => Tokens)
// Parser      (Tokens    => AST)
// Interpreter (AST       => Execution)

let a = 1 + 2 * 3;   // 7
let b = (1 + 2) * 3; // 9
let c = a + b;       // 16
let d = { "hello world"} * 3  // hello worldhello worldhello world
let d = { a = 1; b = 2; a + b; } * 3 // 9

for range 10 {
    print(it);
}

10 + 4 => 14

let x(a) = {
    for range 1..100 {
        a *= it;
    }
    a;
};

let y = if x > 100 {
    x * 2;
} else {
    x * 10;
};

let z = {
    a + b;
};

