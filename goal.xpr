// XPR: A STUDY IN eXPRessions
// Lexer       (Raw Text  => Tokens)
// Parser      (Tokens    => AST)
// Interpreter (AST       => Execution)

a = 1 + 2 * 3;   // 7
b = (1 + 2) * 3; // 9
c = a + b;       // 16
d = { "hello world"} * 3  // hello worldhello worldhello world
d = { a = 1; b = 2; a + b; } * 3 // 9

for range 10 {
    print(it);
}

let x(a) = {
    for range 1..100 {
        a *= it;
    }
    a;
};

let y = if x > 100 {
    x * 2;
} else {
    x * 10;
};

let z = {
    a + b;
};

